# Test technique - Parser txt > JSON

Ce projet permet de parser un fichier txt structuré selon le schéma suivant :

```
num_reference (10 caractères);couleur (1 caractère);prix (valeur numérique);taille (valeur entière)
```